version: "3.8"

services:
  sharkiya-event-discovery:
    build: .
    ports:
      - "4084:4084"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
      - PORT=4084
    restart: unless-stopped
