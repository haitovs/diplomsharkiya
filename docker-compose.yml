services:
  app:
    build: .
    container_name: sharkiya-event-discovery
    ports:
      - "4084:4084"
    restart: unless-stopped
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
